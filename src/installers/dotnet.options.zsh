typeset -A defaultOptions=(
  CHANNEL # Default .NET channel to use to install .NET SDK
  "STS"
  ARCH # Which architecture of the SDK to install. Example - <auto>, amd64, x64, x86, arm64, arm...
  "<auto>"
)
